/**
 * jsPDF - PDF generation library
 *
 * INSTALLATION REQUIRED:
 * This file is a placeholder. To install jsPDF locally:
 *
 * Option 1 - npm (recommended):
 *   npm install jspdf
 *   cp node_modules/jspdf/dist/jspdf.umd.min.js browser-extension/libs/
 *
 * Option 2 - Direct download:
 *   Download from: https://github.com/parallax/jsPDF/releases
 *   File: jspdf.umd.min.js (v2.5.x)
 *   Place in: browser-extension/libs/jspdf.umd.min.js
 *
 * The extension works fully without jsPDF (PNG/JPEG export still works).
 * PDF export requires this file to be replaced with the real library.
 */

// Stub to prevent errors when jsPDF is not yet installed
(function(global) {
  'use strict';

  const stub = {
    jsPDF: class jsPDF {
      constructor() {
        console.warn('[SnapShield] jsPDF not installed. PDF export unavailable.');
        console.warn('[SnapShield] See libs/jspdf.umd.min.js for installation instructions.');
      }
      addImage() { return this; }
      save() {
        alert('لتصدير PDF، يجب تثبيت مكتبة jsPDF.\nراجع ملف: libs/jspdf.umd.min.js');
      }
    }
  };

  if (typeof module !== 'undefined' && module.exports) {
    module.exports = stub;
  } else if (typeof define === 'function' && define.amd) {
    define([], () => stub);
  } else {
    global.jspdf = stub;
  }
})(typeof globalThis !== 'undefined' ? globalThis : typeof window !== 'undefined' ? window : this);
