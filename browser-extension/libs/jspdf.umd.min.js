/**
 * jsPDF — ملاحظة: هذا الملف لم يعد مطلوباً
 *
 * تصدير PDF يعمل الآن بمحرك مدمج مباشرةً في editor.js (دون أي مكتبة خارجية).
 * هذا الملف باقٍ فقط لتفادي أخطاء تحميل السكريبت في editor.html.
 */
(function(global) {
  'use strict';
  // stub فارغ — editor.js لا يستدعيه بعد الآن
  const stub = {
    jsPDF: class jsPDF {
      constructor() {}
      addImage() { return this; }
      save()     {}
    }
  };
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = stub;
  } else if (typeof define === 'function' && define.amd) {
    define([], () => stub);
  } else {
    global.jspdf = stub;
  }
})(typeof globalThis !== 'undefined' ? globalThis : typeof window !== 'undefined' ? window : this);
